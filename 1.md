# Payby Codebase Refactor Track

Dokumen ini adalah daftar kerja refactor struktur kode Payby menuju codebase production-grade.

## Guardrails

- [ ] Jangan mengubah landing page atau file landing tanpa permintaan eksplisit.
- [ ] Pertahankan perilaku wallet, Shelby, Aptos, Move registry, dan direct retrieval.
- [ ] Jangan memindahkan logika on-chain tanpa test atau verifikasi build.
- [ ] Jangan memasukkan `soul.md`, `tasks.md`, `memory.md`, atau `1.md` ke commit produk kecuali diminta.
- [ ] Setiap fase harus tetap lulus `npm.cmd run build`.
- [ ] Refactor dilakukan bertahap; hindari rewrite besar tanpa checkpoint yang bisa dipulihkan.

## Baseline

- [x] Landing page sudah terpisah secara visual dari workspace.
- [x] Konfigurasi network sudah berada di `src/config/networks.ts`.
- [x] Logo Payby sudah berada di `src/components/PaybyLogo.tsx`.
- [x] Kontrak Move sudah berada di `contracts/payby_marketplace/`.
- [x] `src/AppRuntime.tsx` sekarang menjadi composition root sekitar 959 baris; page modules, public flows, shared UI primitives, workspace controls, and route loading boundaries sudah dipisahkan.
- [ ] `src/styles.css` dan `src/workspace.css` masih memiliki cascade dan override berlapis.

## Phase 1: Stabilize Boundaries

- [x] Catat semua export, type, helper, hook, dan page component dari `src/AppRuntime.tsx`.
- [x] Kelompokkan kode menjadi `app`, `domain`, `services`, `hooks`, `pages`, `components`, dan `types` tanpa memindahkan perilaku dulu.
- [x] Buat aturan dependency sederhana: UI tidak memanggil fullnode atau Shelby RPC secara langsung.
- [x] Tentukan kontrak data untuk transaction, activity, media metadata, profile, listing, dan receipt.
- [x] Tambahkan checkpoint build setelah batas modul awal selesai.

## Phase 2: Extract Network and Chain Services

- [x] Buat `src/services/aptos/fullnode.ts` untuk request fullnode, headers, transaction polling, dan error mapping.
- [x] Buat `src/services/aptos/wallet.ts` untuk network alignment dan wallet transaction helpers.
- [x] Buat `src/services/shelby/storage.ts` untuk blob path, direct download, share link, explorer link, Shelby URI, dan retrieval helpers.
- [x] Buat `src/services/payby/marketplace.ts` untuk Move view calls, listing, access policy, purchases, sales, dan profile.
- [x] Buat `src/services/payby/transaction-history.ts` untuk cache, live verification, stale cleanup, dan wallet scoping.
- [x] Pastikan service hanya menerima input terstruktur dan mengembalikan error yang bisa ditampilkan UI.
- [x] Tambahkan fixture untuk response success, pending, failed, 404, 410, 429, dan 5xx.
- [x] Pindahkan adapter React `useUploadBlobs` dan `useDeleteBlobs` ke boundary lifecycle hook/service pada Phase 3 agar lifecycle SDK tetap aman.

### Phase 2 Checkpoint

- [x] Kontrak data lintas fitur berada di `src/domain/models.ts`.
- [x] `AppRuntime.tsx` memakai service Aptos, marketplace, Shelby, dan transaction history yang sudah diekstrak.
- [x] Fallback owner/legacy Move view tetap dipertahankan.
- [x] Status 404/410 menghapus bukti transaksi yang sudah tidak ada; 429/5xx tetap disimpan sebagai cache tidak terverifikasi.
- [x] Landing page tidak disentuh.
- [x] `npm.cmd run build` lulus setelah ekstraksi.

## Phase 3: Extract Persistence and Hooks

- [x] Buat wrapper storage typed untuk `localStorage` dengan parsing aman dan version migration.
- [x] Pindahkan `useStoredMetadata` ke `src/hooks/useStoredMetadata.ts`.
- [x] Pindahkan `useCreatorProfile` ke `src/hooks/useCreatorProfile.ts`.
- [x] Pindahkan `useActivityFeed` ke `src/hooks/useActivityFeed.ts`.
- [x] Pindahkan `usePendingPublishes` ke `src/hooks/usePendingPublishes.ts`.
- [x] Pindahkan `useTransactionHistory` ke `src/hooks/useTransactionHistory.ts`.
- [x] Pindahkan `usePurchaseReceipts` ke `src/hooks/usePurchaseReceipts.ts`.
- [x] Pastikan semua hook tetap wallet-scoped dan network-scoped.
- [x] Pastikan cache hanya menjadi recovery layer; state canonical tetap berasal dari chain atau Shelby.

### Phase 3 Checkpoint

- [x] Cache key lama dipertahankan untuk metadata, profile, activity, pending publish, transaction history, dan purchase receipts.
- [x] `AppRuntime.tsx` tidak lagi memiliki implementasi hook persistence utama.
- [x] Buyer library tetap memuat purchase index on-chain sebelum menggabungkan receipt lokal.
- [x] Landing page tidak disentuh.
- [x] `npm.cmd run build` lulus setelah ekstraksi hook.

## Phase 4: Extract Application Routing and Pages

- [x] Pindahkan route parsing dan navigation ke `src/app/router.ts` atau router module yang setara.
- [x] Buat `src/pages/workspace/VaultPage.tsx`.
- [x] Buat `src/pages/workspace/PublishPage.tsx`.
- [x] Buat `src/pages/workspace/MediaDetailPage.tsx`.
- [x] Buat `src/pages/workspace/AnalyticsPage.tsx`.
- [x] Buat `src/pages/workspace/DiscoverPage.tsx`.
- [x] Buat `src/pages/workspace/LibraryPage.tsx`.
- [x] Buat `src/pages/workspace/NetworkPage.tsx`.
- [x] Buat `src/pages/workspace/ProfilePage.tsx`.
- [x] Buat `src/pages/workspace/ActivityPage.tsx`.
- [x] Buat `src/pages/public/CreatorPage.tsx` dan `src/pages/public/MediaPage.tsx`.
- [x] Buat `src/pages/workspace/WorkspacePage.tsx` sebagai shell workspace tanpa menyentuh landing page.
- [x] Pertahankan landing page sebagai boundary tersendiri tanpa redesign.
- [x] Pastikan setiap page hanya mengatur composition dan page-level behavior.

### Phase 4 Checkpoint - 10 August 2026

- [x] Route parsing, browser history, reduced-motion fallback, and View Transition handling live in `src/app/router.ts`.
- [x] Analytics, Discover, Library, Network, and Activity are rendered from dedicated workspace page modules.
- [x] Shared formatter, empty-state, and pagination ownership moved out of `AppRuntime.tsx`.
- [x] New page modules receive chain/storage behavior through typed props or existing services; no landing markup was changed.
- [x] `npm.cmd run build` passes after the router and page extraction slice.
- [x] Publish, media detail, public creator/media, dan helper/import cleanup sudah selesai dipindahkan dari inline runtime.

### Phase 4 Checkpoint - 10 August 2026 (continued)

- [x] `VaultPage.tsx` sekarang owns Shelby blob listing, creator registry recovery, pending publish queue, pagination, dan wallet-scoped delete/share actions.
- [x] `ProfilePage.tsx` sekarang owns profile editing, avatar optimization, on-chain profile commit, and creator link actions.
- [x] `WorkspacePage.tsx` sekarang owns sidebar, topbar, route title, network control slot, theme control slot, wallet control slot, dan page transition boundary.
- [x] `AppRuntime.tsx` tetap menjadi composition root dan turun menjadi sekitar 1.184 baris; old inline page definitions sudah dihapus.
- [x] `npm.cmd run build` lulus setelah ekstraksi Vault, Profile, dan workspace shell.
- [x] Smoke route `/` dan seluruh delapan route workspace mengembalikan HTTP 200; `git diff --check` tidak menemukan whitespace error.
- [x] Publish, media detail, public creator/media sudah dipindahkan; dead helper/import cleanup tetap menjadi pekerjaan lanjutan.

### Phase 4 Checkpoint - Public Boundaries - 10 August 2026

- [x] `PublishPage.tsx` owns the Shelby upload form, publish lifecycle, progress state, and transaction feedback.
- [x] `MediaDetailPage.tsx` owns creator media detail, preview, retention actions, and republish entry points.
- [x] `CreatorPage.tsx` owns public creator discovery, on-chain listing recovery, metadata recovery, and creator navigation.
- [x] `MediaPage.tsx` owns public media retrieval, Aptos access checks, paid unlock, receipt state, and direct Shelby retrieval.
- [x] Shared `MediaPreview.tsx` and `PurchaseReceiptCard.tsx` are used by public and workspace media flows.
- [x] `AppRuntime.tsx` renders the public modules through typed wallet, metadata, and explorer callbacks; it no longer defines public page bodies.
- [x] `npm.cmd run build` passes after the public boundary extraction.
- [x] Helper/import cleanup dan route-level page loading sudah selesai pada Phase 5 awal.
- [ ] Shared form/status primitives dan CSS consolidation masih menjadi pekerjaan berikutnya.

## Phase 5: Extract Reusable UI Components

- [x] Buat primitives untuk `Button`, `IconButton`, dan `PageHeader` dengan kontrak visual workspace yang sudah ada.
- [x] Buat `FormField` dan `StatusPill` untuk menjaga markup form dan transaction status tetap konsisten.
- [ ] Buat primitives untuk `Input`, `Select`, `Textarea`, dan `Dialog` bila boundary behavior-nya sudah teruji.
- [x] `WorkspacePage` menjadi shell workspace; `PageHeader`, `EmptyState`, dan `PaginationControls` sudah memiliki boundary komponen.
- [ ] Buat boundary terpisah untuk `Sidebar`, `Topbar`, dan `DataRail` setelah kontrak CSS-nya dipetakan.
- [ ] Buat record components untuk transaction row, activity row, media row, listing row, dan pagination.
- [ ] Buat feedback components untuk loading, error, success, wallet state, network state, dan transaction state.
- [ ] Semua interactive component wajib memiliki default, hover, focus-visible, active, disabled, loading, dan error state.
- [ ] Pastikan komponen tidak mengubah ukuran layout saat hover, focus, atau loading.
- [ ] Pastikan keyboard navigation dan touch target tetap valid setelah ekstraksi.

### Phase 5 Checkpoint - 10 August 2026

- [x] `src/components/workspace/PageHeader.tsx`, `Button.tsx`, dan `IconButton.tsx` ditambahkan tanpa mengubah landing page.
- [x] `src/components/workspace/FormField.tsx` dan `StatusPill.tsx` ditambahkan dengan kontrak markup yang mempertahankan selector workspace yang sudah ada.
- [x] `src/components/workspace/WorkspaceControls.tsx` owns theme, network, and wallet controls shared by workspace and public pages.
- [x] Semua workspace page header memakai `PageHeader`; action yang sudah dimigrasikan memakai `Button` atau `IconButton`.
- [x] Workspace dan public page modules memakai `React.lazy` dengan `Suspense` fallback yang eksplisit; `WorkspacePage` tetap menjadi shell sinkron.
- [x] Production build mengeluarkan route chunks untuk workspace dan public pages; warning main chunk masih dicatat untuk ekstraksi provider/service berikutnya.
- [x] Smoke route mengembalikan HTTP 200 untuk landing, delapan workspace routes, creator, dan media SPA paths.
- [x] Landing files tetap tidak berubah dan `git diff --check` tidak menemukan whitespace error.

## Phase 6: Reorganize CSS Safely

- [x] Pisahkan design tokens dan theme variables ke `src/workspace-tokens.css`.
- [x] Pisahkan core shell/layout rules ke `src/workspace-base.css` dan page component rules ke partial terpisah.
- [x] Pisahkan shared elements ke `src/workspace-elements.css`, responsive rules ke `src/workspace-responsive.css`, dan state/layout refinement ke partial berurutan.
- [x] Pertahankan `landing.css` sebagai boundary tersendiri.
- [ ] Hapus selector lama hanya setelah tidak lagi digunakan.
- [ ] Hapus override duplikat dan selector dengan specificity tidak perlu.
- [x] Jangan menambah `!important` untuk menambal cascade.
- [ ] Verifikasi dark mode dan light mode pada seluruh workspace.
- [ ] Verifikasi 320 px, 768 px, 1440 px, dan wide desktop.

### Phase 6 Checkpoint - 10 August 2026

- [x] `src/workspace.css` sekarang menjadi import boundary berurutan; urutan cascade lama dipertahankan.
- [x] Partial CSS yang aktif: `workspace-tokens`, `workspace-base`, `workspace-elements`, `workspace-components`, `workspace-responsive`, `workspace-foundation`, `workspace-composition`, dan `workspace-states`.
- [x] Build TypeScript/Vite lulus dan seluruh partial CSS memiliki jumlah opening/closing brace yang seimbang.
- [x] Smoke HTTP mengembalikan 200 untuk landing, delapan workspace routes, creator, dan media SPA paths.
- [x] `src/landing.css`, `src/main.tsx`, landing screenshot asset, dan `public/payby-icon.svg` tetap tidak berubah.
- [x] Screenshot QA dark/light pada seluruh delapan route workspace telah diperiksa pada 320, 768, 1440, dan wide desktop 1920px tanpa horizontal overflow.
- [x] Static selector audit selesai; tujuh kandidat tanpa token sumber semuanya merupakan state class dinamis dan dipertahankan.
- [ ] Hapus selector lama atau deduplicate override historis hanya jika ada bukti penggunaan dan cascade replacement yang aman.

### Phase 6 Checkpoint - Visual QA and Selector Audit - 10 August 2026

- [x] Mobile Profile diperbaiki: sidebar sekarang satu baris horizontal yang dapat digeser, dan side link tidak lagi memakai lebar `100%` pada viewport sempit.
- [x] Guard responsive workspace ditambahkan di `workspace-states.css` untuk menjaga topbar, control, page grid, form, dan profile/publish layout tetap shrinkable.
- [x] Visual spot check dark Vault 1440px dan light Profile 1440px selesai; light Profile 320px juga diperiksa.
- [x] QA matrix mencakup Vault, Publish, Analytics, Discover, Library, Network, Profile, dan Activity pada dark/light di 320/768/1440/1920px.
- [x] Landing boundary tetap bersih; `src/landing.css`, `src/main.tsx`, landing screenshot asset, dan `public/payby-icon.svg` tidak berubah.
- [x] Audit selector workspace menemukan hanya `is-failed`, `is-pending`, verification state, dan status-pill variants sebagai kandidat dinamis; tidak ada selector yang aman dihapus.

## Phase 7: Test and Quality Gates

- [x] Tambahkan test untuk pure route parsing dan path serialization yang dipakai oleh route transitions.
- [x] Tambahkan test untuk wallet scoping antara wallet A dan wallet B.
- [x] Tambahkan test untuk network switching antara Shelbynet dan Shelby Testnet.
- [x] Tambahkan test untuk transaction lifecycle: pending, confirmed, failed, missing, unavailable.
- [ ] Tambahkan test untuk publish recovery dan retry.
- [x] Tambahkan test untuk access policy mapping, chain listing recovery, dan unsupported gate blockers.
- [ ] Tambahkan test integrasi media access: free, allowlist, dan paid.
- [ ] Tambahkan test untuk profile and creator page consistency.
- [x] Jalankan `npm.cmd run build`.
- [ ] Jalankan `npm.cmd run check:readiness` sampai Shelbynet marketplace module callable.
- [x] Jalankan smoke test untuk semua route workspace pada Phase 5/6; pengulangan lokal tetap dibutuhkan setelah deployment terakhir.
- [ ] Jalankan real E2E dengan wallet creator dan wallet buyer.
- [ ] Review console errors, failed network calls, accessibility, dan overflow sebelum release.

### Phase 7 Checkpoint - 12 August 2026

- [x] Added `tests/router.test.ts`, `tests/wallet-network.test.ts`, `tests/transaction-history.test.ts`, and `tests/access-and-storage.test.ts`.
- [x] Added `npm.cmd run test` with 19 deterministic Node tests.
- [x] `npm.cmd run build` passes; the existing large `AppRuntime` chunk warning remains tracked.
- [x] Readiness check now uses the same direct Shelby fullnode host and API-key header model as the runtime.
- [x] Pre-deployment readiness output correctly distinguished the old Shelbynet blocker; the deployment verification below resolved it.
- [ ] Do not mark Phase 7 complete until the Shelbynet contract is deployed/configured and real creator/buyer E2E passes.

### Phase 7 Deployment Blocker - 12 August 2026

- [x] Compile `contracts/payby_marketplace` successfully.
- [x] Harden `scripts/deploy-payby-marketplace.ps1` so it requires a dedicated Payby profile or local signer file, rejects `karya-*` and `yora-*`, uses direct Shelby, and selects the correct network API key.
- [x] Verify the loaded Aptos CLI profile list; a workspace-only profile is not treated as deployable until the CLI can actually load it.
- [x] Create or provide a dedicated, funded Payby publisher signer without sharing its private key in the repository or chat.
- [x] Publish and initialize `payby_marketplace` on Shelbynet, then rerun readiness.

### Phase 7 Deployment Attempt Before Funding Correction - 12 August 2026

- [x] Confirm the old Payby testnet account was not the Shelbynet deployer; use the dedicated Payby signer for Shelbynet.
- [x] Test Shelbynet faucet connectivity with a disposable key; the endpoint refused the connection.
- [x] Remove the unused disposable key after the failed funding attempt.
- [x] Confirm no publish or initialize transaction was sent during the unfunded preflight.

### Phase 7 Verification - 12 August 2026

- [x] `npm.cmd run test`: 19 tests passed.
- [x] `npm.cmd run build`: passed; the known `AppRuntime` chunk warning remains.
- [x] The pre-deployment readiness check correctly remained non-zero because the old Shelbynet address had no module.
- [x] Deploy script parser and fail-closed signer/profile guards passed.
- [x] Final deploy-script pass leaves no temporary keypair under `.aptos/` and keeps local signer paths outside repository contents.
- [x] Generate a fresh dedicated Payby publisher signer locally without exposing the private key.
- [x] Fund the dedicated publisher address on Shelbynet and confirm the balance through the indexed fungible-asset endpoint.
- [x] Advance readiness only after the signer is funded and the deployed Shelbynet module is callable.

### Funding Verification Correction - 12 August 2026

- [x] Verify the public address against the local Ed25519 signer with the Aptos SDK.
- [x] Shelbynet GraphQL `current_fungible_asset_balances` reports `30 APT` and `0.2 SHELBY_USD` for the dedicated Payby signer.
- [x] The missing legacy APT `CoinStore` was identified as an endpoint/model mismatch, not proof of zero balance.
- [x] Publish and initialize the module, then verify the deployed module through the readiness check.

### Phase 7 Deployment Verification - 12 August 2026

- [x] Published `payby_marketplace` at `0x962ebbcf81cbc5dc0950a8ca036d54828481043f1df8960a2ec4d50fae8c3a12`.
- [x] Publish transaction finalized: `0x7f2615f639e16c11b42e7ae990edbcc66fd0c66e18e646d1cffd9f214dd60965`.
- [x] Initialized the registry with a `100000` gas-unit limit after the first `20000` gas-unit attempt correctly failed out of gas.
- [x] Initialize transaction finalized: `0x6cb19b17efc004e6c6ecb52c38c875e16c677a342ed76dc2d48e524f9779f3946`.
- [x] Updated local Shelbynet configuration to the dedicated Payby address.
- [x] `npm.cmd run check:readiness` passes for Shelbynet and Shelby Testnet.
- [ ] Complete real creator/buyer wallet E2E and browser QA before community release.

### Phase 7 Runtime Configuration and Verification - 12 August 2026

- [x] Added the live public Shelbynet marketplace address as a safe frontend fallback; Vercel no longer depends on a local ignored `.env` for this public address.
- [x] Updated the Shelbynet indexer display endpoint to `https://api.shelbynet.shelby.xyz/v1/graphql`.
- [x] Added an Aptos TypeScript SDK deployment path for local signer files; the CLI profile path remains available for dedicated profiles.
- [x] `node --check scripts/deploy-payby-marketplace.mjs` passes.
- [x] `npm.cmd run test`, `npm.cmd run build`, `npm.cmd run check:readiness`, and `git diff --check` pass.

## Phase 8: Cleanup and Release

- [ ] Hapus helper dan import yang tidak lagi dipakai.
- [ ] Hapus generated artifact yang tidak seharusnya dilacak.
- [ ] Pastikan secrets hanya berada di environment variables.
- [ ] Perbarui README dengan struktur folder final dan cara menjalankan test.
- [ ] Perbarui `soul.md`, `tasks.md`, dan `memory.md`.
- [ ] Review `git diff` dan `git diff --check`.
- [ ] Buat commit kecil per fase dengan pesan yang jelas.
- [ ] Push ke GitHub hanya setelah user meminta atau menyetujui push.

## Definition of Done

- [ ] `AppRuntime.tsx` tidak lagi menjadi tempat seluruh domain logic dan seluruh page component.
- [ ] UI layer tidak mengetahui detail endpoint fullnode, Shelby RPC, atau Move function ID.
- [ ] Service layer dapat diuji tanpa merender React.
- [ ] Page components dapat dibaca dan dimodifikasi tanpa menelusuri ribuan baris.
- [ ] CSS workspace memiliki satu sumber aturan utama dan tidak bergantung pada override acak.
- [ ] Landing page tetap identik secara perilaku dan visual kecuali ada permintaan eksplisit.
- [ ] Build, readiness check, wallet flow, Shelby flow, dan buyer flow lulus.
- [ ] Tidak ada data cache yang ditampilkan sebagai sumber kebenaran on-chain.

### Publish SDK Schema Repair - 12 August 2026

- [x] Replace retired Shelby React query/mutation names with the SDK 0.7 API.
- [x] Align the client with the live modern Shelbynet GraphQL schema.
- [x] Prevent retired Shelby Testnet from creating a misleading Shelbynet client.
- [x] Verify the installed SDK account-blob query against the live Shelbynet endpoint.
- [x] Re-run build, deterministic tests, whitespace checks, and landing-boundary checks.
- [ ] Complete real browser publish E2E; this is the next release gate.
- [x] Re-run readiness after the SDK migration; configured Move views remain callable.

### Blank Screen Runtime Recovery - 12 August 2026

- [x] Diagnose the browser import failure as a stale Vite dependency cache.
- [x] Confirm `@shelby-protocol/react@4.1.0` publicly exports `useDeleteObjects` and `useFullObjectMetadata`.
- [x] Clear the generated Vite cache and restart the dev server on port 5173 with `--force`.
- [x] Confirm the regenerated prebundle contains the current hook exports.
- [x] Confirm `/app/vault` and the workspace module return HTTP 200.
- [x] Re-run `npm.cmd run build`, `npm.cmd run test`, `git diff --check`, and landing-boundary verification.
- [ ] Run the real wallet-backed publish flow; this is still the next functional release gate.

### Shelby Registration Location Fix - 12 August 2026

- [x] Confirm the live Shelbynet placement name: `shelbynet-1`.
- [x] Add the location hint to network configuration and `.env.example`.
- [x] Pass the hint through `useUploadBlobs` so Petra receives a valid registration payload.
- [x] Add a regression test covering the generated batch registration arguments.
- [x] Pass build, 20 tests, and whitespace validation.
- [ ] Retry the funded browser publish and verify the full Shelby lifecycle.

### Petra Registry Prompt Repair - 12 August 2026

- [x] Include an explicit empty generic-argument array in every Payby entry-function transaction payload.
- [x] Normalize Shelby upload transaction payloads before the Aptos wallet adapter receives them.
- [x] Keep publish progress in registry state until Aptos listing transactions are finalized.
- [x] Preserve failed registry items and expose a retry path instead of displaying false success.
- [x] Validate metadata commitments and transaction hashes at the registry boundary.
- [x] Mark registry transactions failed when finality polling fails after submission.
- [x] Add a regression test and pass 22 tests plus the production build.
- [ ] Complete the funded Petra publish retry and verify the full Shelby/Aptos lifecycle.

### Petra Chain-ID Mismatch Diagnosis - 12 August 2026

- [x] Verify live Shelbynet chain ID `118` from the configured fullnode.
- [x] Verify installed Petra `2.5.0` native Shelbynet chain ID `59` from its local network configuration.
- [x] Verify the deployed registry function and arguments against the live module ABI.
- [x] Verify chain `59` produces `BAD_CHAIN_ID` while chain `118` produces a normal VM response.
- [x] Correct the SDK builder to use `Network.SHELBYNET`.
- [x] Add a live wallet-chain preflight before Shelby upload and registry retry.
- [x] Record that Petra raw signing still performs wallet-side simulation; it is not a bypass.
- [ ] Update Petra or select a wallet network profile that reports chain ID `118`.
- [ ] Validate a funded Petra publish after the wallet reports chain ID `118`.
